/* Define a macro swap(t,x,y) that interchanges two arguments of type t.
Block structure will help */

#include <stdio.h>

void swap(t, x, y) {

}

int main(void) {
    
    swap(t,x,y);
    
    return 0;
}